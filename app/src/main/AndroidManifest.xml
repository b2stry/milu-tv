<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shallowan.milutv">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:name=".MiluApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_milu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_milu"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".welcome.WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBarWithBackGround">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".register.RegisterActivity" />
        <activity android:name=".register.PhoneRegisterActivity" />
        <activity android:name=".editprofile.EditProfileActivity" />
        <activity android:name=".webview.WebViewActivity" />
        <activity android:name=".main.MainActivity" />
        <activity android:name=".register.ForgetActivity" />
        <activity
            android:name=".hostlive.HostLiveActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".watcher.WatcherActivity" />
        <activity android:name=".createroom.CreateLiveActivity" />

    </application>

</manifest>